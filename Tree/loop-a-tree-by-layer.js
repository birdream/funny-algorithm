// 广度优先算法 BFS
const solution_BFS = (node) => {
    if (!node) { return [] }

    const result = [];
    const queue = [];
    queue.push(root);

    // const visited = set(root)

    while (queue.length) {
        const level_size = queue.length;
        const current_level = [];

        for (let i = 0; i < level_size; i++) {
            const element = array[i];

        }
    }
}

// 深度优先 DFS
const solution_DFS = () => {}

;(() => {

})()